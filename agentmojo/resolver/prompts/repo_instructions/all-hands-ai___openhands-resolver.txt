This is a Python repo for agentmojo-resolver, a library that attempts to resolve github issues with the AI agent AgentMojo.

- Setup: `poetry install --with test --with dev`
- Testing: `poetry run pytest tests/test_*.py`
